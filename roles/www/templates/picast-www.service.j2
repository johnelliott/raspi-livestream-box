[Unit]
Description=picast server
After=network.target

[Service]
EnvironmentFile=/home/{{ user }}/picast/.env
Type=simple
ExecStart=/home/{{ user }}/.nvm/versions/node/{{ node_version }}/bin/node /home/{{ user }}/picast/bin/www
Restart=on-failure
RestartSec=1

[Install]
WantedBy=multi-user.target
